from setuptools import setup, find_packages

setup(
    name='aoc',
    author='Alex Cannan',
    author_email='alexfcannan@gmail.com',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'requests',
    ],
)